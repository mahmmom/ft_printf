#include "ft_printf.h"

int ft_printf(const char *format, ...)
{
while (*)
{
  if (*format == '%')
    format++;

while (*format)
{
  if (*format == '%')
    format++;
      if (*format == '%"){
          Putchar('%')
            counter++;
}

  
  
}
